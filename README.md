# Auth library
There are many options to deal with authentication like OAuth, OpenID, but are they simple? What if you need to create basic product that simply works?

Well, no worries. This library offers simple login-password authentication with JWT tokens usage. No need to worry about user authentication anymore.

